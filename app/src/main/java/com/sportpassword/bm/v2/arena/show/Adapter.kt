package com.sportpassword.bm.v2.arena.show

import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import com.sportpassword.bm.databinding.ShowArenaBinding

class Adapter(private val viewModel: ViewModel): RecyclerView.Adapter<Adapter.ViewHolder>() {
    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ViewHolder {
        TODO("Not yet implemented")
    }

    override fun getItemCount(): Int {
        TODO("Not yet implemented")
    }

    override fun onBindViewHolder(holder: ViewHolder, position: Int) {
        TODO("Not yet implemented")
    }

    inner class ViewHolder private constructor(private val binding: ShowArenaBinding): RecyclerView.ViewHolder(binding.root) {

    }
}